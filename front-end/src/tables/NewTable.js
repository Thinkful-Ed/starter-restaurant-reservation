import React from "react";
import TableForm from "./TableForm";

/**
 * Defines the new table page.
 * @returns {JSX.Element}
 */

function NewTable() {
  return (
    <div>
      <h1>Create a New Table</h1>
      <TableForm />
    </div>
  );
}

export default NewTable;
